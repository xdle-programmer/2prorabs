section.basket-empty
    .container
        .breadcrumbs
            .breadcrumbs__inner
                a.breadcrumbs__link(href="#") Главная
                .breadcrumbs__separator
                a.breadcrumbs__link.active(href="#") Корзина
        .basket-empty__inner
            .title.basket-empty__title В корзине пока ничего нет
            img(src="/src/blocks/basket-empty/assets/img/empty-box.png").basket-empty__img
            .basket-empty__text
                | Воспользуйтесь поиском, чтобы найти нужный товар или
                a(href="#").basket-empty__link перейдите в каталог.
